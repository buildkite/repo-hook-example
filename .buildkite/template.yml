name: "Repo Hook Example"
description: "An example pipeline that sets environment variables using a repository hook."
emoji: ":hook:"
languages:
  - "Shell"
steps:
  - command: "buildkite-agent pipeline upload"
    label: ":pipeline:"
